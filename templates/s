git config --global user.name "Your Name"
git config --global user.email "your_email@example.com"
git config --list





🚀 PART 2 — Create a Shared Repository

One teammate (say you) creates a new repository on GitHub:

Click New Repository

Choose a name (e.g., flask-whatsapp-tool)

Select Public or Private

Initialize with a README (optional)

Copy the repo URL (looks like https://github.com/yourname/flask-whatsapp-tool.git).

👯‍♂️ PART 3 — Add Your Teammate

On the repo page → Settings → Collaborators

Add your teammate’s GitHub username or email.

They’ll get an invitation → accept it.

Now both can edit the same repo!


🖥️ PART 4 — Clone the Repo (both teammates do this)

In your terminal or VS Code:

git clone https://github.com/yourname/flask-whatsapp-tool.git
cd flask-whatsapp-tool